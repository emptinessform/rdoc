# 2026-08-23 — S31: 리스트 편집 (M1)

## 목표·범위

글머리표/번호 리스트 토글(툴바+메뉴), Tab/Shift+Tab 수준 증감,
Enter 시 리스트 연속 — Word 동작.

## 한 일

- **포크 2커밋** (핀 4ac3abe):
  - 5239e78 `Document::ensure_list_num(bullet)` — add_*_list_item에
    사적으로 갇혀 있던 공유 리스트 정의 find-or-create를 공개 API로
    추출 (기존 문단을 리스트로 토글하려면 문단 추가 없이 numId가
    필요). 두 item 헬퍼는 위임으로 단순화.
  - 4ac3abe `Paragraph::numbering_value()` 게터 (빌더 쪽 —
    ParagraphRef::numbering의 쌍).
  - 그 외 필요 부품은 업스트림에 이미 있었음: set_numbering_value,
    numbering_is_bullet, CT_Numbering::add_bullet/numbered_list.
- **rdoc lib**: `list_numbering_at` / `set_list_at` (6스토리 공용
  관문).
- **wasm 3종**: `toggle_list_paths(paths, bullet)` — Word 의미론
  (전부 해당 종류면 해제, 혼합/무이면 전부 설정, 수준 보존; 1 undo),
  `list_info(path)` (읽기), `set_list_level(path, delta)` (0..=8
  클램프, 1 undo).
- **UI**: 툴바 •¶/1. 버튼(+눌림 상태 표시), 서식 메뉴 항목,
  Tab/Shift+Tab — 표 셀 탐색이 우선이고 그 외에서 리스트 문단이면
  수준 증감. 토글은 마커 히트가 추가/제거되므로 선택은 시작 캐럿으로
  붕괴(캐럿은 텍스트 오프셋 불변이라 유지).

## 검증 (실측)

- 신규 list-test 13단정: 평문→불릿(마커 "•" 렌더 + 캐럿 생존) →
  재토글 해제 → undo 복원 → Tab 수준 1(마커 들여쓰기 실측) →
  Shift+Tab 복귀 → **Enter가 리스트 연속**(분할이 pPr 승계 — 별도
  수정 없이 동작 확인) → 두 문단 번호 리스트(마커 "1."/"2.") →
  저장 왕복 보존.
- 전체 배터리 **35/35 그린** (플레이크 0), 네이티브 lib 9/9 +
  corpus 14/14.
- 증거 docs/evidence/s31-lists.png — 레벨 1 불릿이 Word 규약대로
  ○ 마커, 번호 1./2., 툴바 "1." 눌림 상태.

## 한계·후속

- 리스트 안 Enter로 빈 항목에서 한 번 더 Enter 시 리스트 탈출(Word
  동작)은 미구현 — 후속 소품.
- 표 셀 안 리스트는 Tab이 셀 탐색에 양보 (수준 변경은 툴바로).
- 스위트 사고 1건: undo가 캐럿을 비우는 걸 잊고 Tab을 눌러 2단정
  실패 — 캐럿 재배치로 수정 (스위트 버그, 제품 정상).
