# 2026-08-23 — S29: 폰트 패밀리 적용 (M1 착수)

## 목표·범위

M1 첫 기능: 선택 범위에 폰트 패밀리 적용 (툴바 글꼴 드롭다운).
S26~S28 폰트 인프라(오픈 폰트 별칭·볼드 단면·합성 B/I)와 직결.

## 한 일

- rdoc lib `set_family_in`: set_size_in과 같은 런 분할 골격, 커버된
  런에 upstream `Run::set_font` (rFonts ascii/hAnsi/eastAsia/cs 동시
  설정 — Word의 글꼴 드롭다운과 같은 의미론).
- wasm `set_family_ranges(json, family)`: 임의 선택 모양(같은 문단/
  형제/셀 간 scatter)을 문단별 구간으로 받아 1 히스토리 엔트리.
- UI: 툴바 `#fontfamily` 드롭다운 (Pretendard/맑은 고딕/나눔명조/
  굴림/돋움/바탕 — S26 매핑 세트와 정합). 패밀리는 리플로우를
  유발하므로 적용 후 선택은 구간 시작 캐럿으로 붕괴 (크기와 동일).
  `__t.fontFamily` 훅.
- fonts-test 확장: 굴림(산세리프) 문단 전체를 선택해 "바탕" 적용 →
  총폭이 세리프 문단과 정확히 일치, undo 1회로 산세리프 폭 복원.

## 검증 (실측)

- fonts-test(적용/undo 단정 포함) PASS, 전체 배터리 **33/33 그린**
  (플레이크 0).
- 네이티브 lib 9/9, corpus 14/14.

## 한계·후속

- 드롭다운이 캐럿 위치의 현재 글꼴을 표시하지 않음(적용 후 플레이스
  홀더로 복귀) — 툴바 상태 표시(B/I/U on/off 포함)와 함께 M1 후속.
- 목록의 6종 외 임의 폰트명 입력은 미지원 (콤보박스화 후속).
