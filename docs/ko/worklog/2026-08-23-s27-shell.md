# 2026-08-23 — S27: 제품 셸 v1 (메뉴바 + 툴바)

## 목표·범위

rhwp 수준의 첫인상 — 데모 공개의 전제로 사용자가 짚은 "메뉴·툴바
부재"를 해소한다. 편집 로직 무변경: 기존 명령 함수에 메뉴를 배선만.

## 한 일

- **메뉴바** (rdoc | 파일 편집 삽입 서식 보기 | status):
  - 파일: 데모 열기(#demo) / 열기…(#file 트리거) / .docx 저장(#save)
  - 편집: 실행 취소·다시 실행, 잘라내기·복사·붙여넣기·모두 선택,
    찾기·바꾸기 — 단축키를 kbd로 항목에 표기 (기존의 빽빽한 힌트
    한 줄을 대체)
  - 삽입: 이미지…, 각주/미주/노트 삭제, 표 행·열 4종
  - 서식: 굵게/기울임/밑줄, 정렬 3종 · 보기: 페이지 목록(#thumbtoggle)
  - 메뉴 항목 "붙여넣기"는 navigator.clipboard.readText 시도, 권한
    거부 시 "Ctrl+V를 사용하세요" 안내 (프로그램적 읽기는 실제
    Ctrl+V에 없는 권한이 필요).
- **드롭다운 동작** (`web/src/menu.ts`): 클릭 토글, 열린 상태에서
  다른 제목 hover 시 전환, 바깥 클릭/Esc 닫힘. mtitle mousedown은
  preventDefault로 에디터 캐럿/포커스 보존.
- **툴바 재구성**: B/I/U 버튼 신설(#fmtbtns — 기존엔 단축키만 있었음)
  + 정렬/크기/색/스타일/표/이미지/줌 그룹화(구분선), 다크 테마 통일
  스타일. 파일 input 2종은 숨김으로 이동.
- 테스트가 참조하는 id 전부 보존 (#demo/#save는 메뉴 항목으로 이동
  — 프로그램적 .click()은 display:none에서도 동작).

## 검증 (실측)

- 메뉴 스모크: 열림/hover 전환/Esc 닫힘 3단정 통과, 편집>실행 취소로
  타이핑 롤백 확인 (menu 경로로 undo 동작).
- 전체 배터리 **33/33 그린** (스위트 무수정, 전경 2분할 실행 —
  이번엔 플레이크 0).
- 데모 로드 101ms / 이후 41ms. 스크린샷
  docs/evidence/s27-shell.png (편집 메뉴 열린 상태).

## 한계·후속

- 모바일(터치/좁은 폭) 대응은 별도 단계 — 메뉴바는 wrap만 됨.
- 툴바 버튼 활성 상태 표시(캐럿 위치의 B/I/U on/off 반영)는 M1 후보
  (ranges_format_on 읽기는 이미 있음).
- 도움말/정보 메뉴(버전, GitHub 링크)는 후속.
