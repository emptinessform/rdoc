# 2026-08-23 — S23-3: 테스트 러너 공식화 (M0 마감)

## 목표·범위

ad-hoc 셸 루프로 돌리던 브라우저 배터리를 저장소의 공식 러너
`web/tests/run.sh` 하나로 고정한다. 스위트 파일은 무수정.

## 한 일

- `web/tests/run.sh`: 전체(`run.sh`) / 부분(`run.sh ime paste`,
  `-test.js` 접미사 생략 가능) 실행, 사전 점검(서버 미기동 / browse
  바이너리 부재 / 미존재 스위트 → 종료 코드 2), PASS/FAIL 집계 +
  실패 목록, 실패 시 종료 코드 1. 환경 변수 RDOC_URL /
  RDOC_TEST_SLEEP(기본 16초) / BROWSE_BIN.
- 기존 규약 유지: 스위트는 /tmp로 복사해 주입(browse eval 제약),
  캐시 버스트 URL, **폴링 없이 sleep 후 1회 읽기** (+무결과 시 8초
  유예 1회 — 재시도이지 폴링 아님).
- `web/tests/README.md` 실행 방법을 러너 중심으로 개편 (수동 디버깅
  레시피는 유지).

## 검증 (실측)

- 단일 스위트: `run.sh ime` → PASS, exit 0.
- 합성 실패 스위트(zzfail, 임시 파일): FAIL 표시 + FAILED 목록 +
  exit 1. 미존재 스위트명: ERROR + exit 2.
- 전체 배터리: `run.sh` → **32/32 PASS, exit 0**.

## M0 마감

M0 제품 골격 3단계 완료 — S23-1 워크스페이스화, S23-2 ES 모듈 분할,
S23-3 테스트 러너. 다음은 M1 (에디터 완성도: 리스트 편집, 하이퍼링크,
폰트 패밀리, 줄/문단 간격, 셀 병합/분할, 명령 기반 undo 등에서 선정).
