# 2026-08-23 — 업스트림 응답: #42/#43 → F-X048 편성, #23 종결

메일 5통 (mantissaman, 이슈/PR 코멘트) 요지.

## #42 (이슈) / #43 (드래프트 PR) — dense-form

- 감사(audit) 결과 **결함 7건 모두 현재 main에서 실재** 확인.
- S53에 **F-X048 "Dense form table fidelity"** 로 편성. 커버 범위는
  우리 수정 전체: 중첩 표 블록, 그리드 인지 vMerge, exact 행 높이/클립,
  네임스페이스·스키마 순서 보존 표 스타일 저장+캐스케이드, 셀 앵커
  (behind 레이어 포함), **바깥 경계 Word 동작 고정**(스펙-Word 괴리
  질의에 대한 답 — Word 쪽 채택), 문단 마크 메트릭,
  add_run_inheriting_mark 파사드.
- PR #43은 **머지하지 않음**: 현재 main과 충돌, superseded #41 엔진
  표면 상속, 스키마 순서/네임스페이스/캐시 회계/회귀 강화 필요 —
  S52의 검증·트랜잭션 엔진 위에 **재구현**하고 기여는 기록으로 보존.
- 처리: v0.9.0 안정 릴리스 마일스톤. 릴리스 노트에 #42/#43 링크 +
  @emptinessform 크레딧. F-X048 착지·최종 회귀 통과 시 구현 SHA를
  게시하고 "addressed"로 종결. 그때까지 이슈·PR open 유지.

## #39/#40/#41 — 릴리스 계획 코멘트

F-X039/40/43~47은 v0.9.0(Word 안정 표면)과 rpptx-v0.5.0(공유 계층)
릴리스 노트에 편성, 각각 PR 링크 + 크레딧. 태그별 최종 승인은 별도.

## #23 — 종결

F-X041(업스트림 27a9802)로 해결 — 셰이핑의 유니코드 브레이크 분할을
단일 소유자로 통합, PDF/래스터 회귀 클린. 릴리스 노트에 보고자
@mantissaman + **추가 진단·테스트 케이스 @emptinessform 크레딧**.

## 우리 쪽 함의·조치

- **#43 리베이스 불필요 확정** — 업스트림이 재구현하므로 브랜치
  fx-dense-form-layout은 참조 구현으로 동결.
- 프로덕션은 계속 61f1151 (dense-form 수정은 우리 포크에 이미 있음).
  F-X048/v0.9.0이 나오면: ① 포크에서 dense-form 커밋 폐기 가능 여부
  검증(영수증 1페이지 + corpus 22종 재실측), ② s52 성능 회귀
  재평가 후 이행 결정.
- 보관 중인 성능 피드백 초안(docs/upstream/2026-08-23-s52-perf-feedback-draft.md):
  v0.9.0이 s52 엔진 기반으로 확정된 만큼, **릴리스 전 게시가 회귀
  수정을 같은 마일스톤에 실을 마지막 기회**일 수 있음 — 게시 여부는
  사용자 결정 대기 (수치 재검증 필수).
- 문서 갱신: CLAUDE.md 업스트림 상태, README 업스트림 섹션 (이 파일과
  같은 커밋).

코드 변경 없음, 검증 항목 없음 (기록만).
