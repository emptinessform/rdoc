# 2026-08-23 — S37: 주석 표시 (M2)

## 목표·범위

문서 주석의 읽기 표시 — 보기 메뉴 토글로 앵커 범위 하이라이트,
범위 안에 캐럿을 두면 상태줄에 저자·내용. 작성/삭제는 후속.

## 한 일

- **포크 (핀 91b6a7e)**: `ParagraphRef::comment_spans()` — 런 경계에
  있는 주석 범위 마커를 문자 오프셋으로 변환 (미짝 start는 문단 끝,
  미짝 end는 0에서 열림 — 문단 간 주석은 문단별로 클램프).
  comments()/CommentRef(저자·본문·resolved)는 업스트림에 이미 있었음.
- wasm 읽기 2종: `comment_list()` [{id, author, text, resolved}],
  `comment_spans()` [{id, path, start, end}] — 본문 최상위 문단 대상
  (표 안 주석은 후속).
- UI `comments.ts`: 보기>주석 표시 토글 — 오버레이(연녹색, find
  하이라이트 패턴)를 apply/드레인마다 스팬 재조회로 갱신(편집으로
  오프셋이 밀려도 정확), 캐럿이 범위 안이면 상태줄에
  "주석(저자): 내용" (해결됨 표기 포함).
- 픽스처 web/comment-test.docx (comments.xml 파트 + range 마커 포함,
  파이썬 생성).

## 검증 (실측)

- 신규 comment-test 8단정: 목록(저자/본문), 스팬이 "target"
  정확 커버(6..12), 하이라이트 rect가 앵커 x를 실제 덮음, 캐럿
  진입 시 상태줄 표시/이탈 시 정상, **앞쪽 편집 후 스팬이 7..13으로
  이동**(라이브 재조회 검증), 토글 해제 시 오버레이 0.
- 전체 배터리 **41/41 그린**, 네이티브 lib 9/9 + corpus 14/14.

## 공유 클론 관련 (기록)

- 포크 클론에서 SBOdf 세션이 커밋 2건(dbc756f 한국어 어절 줄바꿈 —
  `hangul_word_wrap` 기본 false로 Word/DOCX 경로 무변경, 1a7a5db
  오블리크 합성)을 푸시 — 이번 핀(91b6a7e)에 상속됨. 41종 배터리
  그린으로 우리 경로 무회귀 확인.
- 빌드 캐시 사고: 두 세션이 같은 target/을 동시 빌드해 증분 캐시
  손상("cached cgu should have an object file") — `cargo clean -p`로
  복구. 공유 클론에서는 동시 빌드 주의.

## 한계·후속

- 표 셀 안 주석 앵커 미보고, 주석 작성/답글/해결 편집은 후속.
- 말풍선/사이드 패널 UI 없음 (상태줄 표시가 v1).
