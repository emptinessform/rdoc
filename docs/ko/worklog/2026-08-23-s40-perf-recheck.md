# 2026-08-23 — S40: 성능 재실측 (중단 — 측정 환경 오염)

## 목표

S29~S39 기능 추가 이후 63페이지 게이트(<30ms) 재검증.

## 측정 시도와 판정 불가 사유

| 실행 (순서대로) | rev | 키 입력 mean/min | 콜드 |
|---|---|---|---|
| 1 | 91b6a7e | 48 / 38ms | 2901ms |
| 2 | 235d56f (SBOdf 이전) | 78 / 58ms | 3245ms |
| 3 | 91b6a7e | 140 / 116ms | 4030ms |
| 4 | 91b6a7e | 139 / 101ms | 4664ms |

- 같은 rev가 실행마다 나빠지고(48→140ms), **더 오래된 rev가 더 느리게**
  나옴 — rev 요인이 아니라 시스템 요인.
- 측정 시점에 rustc/cargo 프로세스 0, 그러나 전체 CPU 50~66% 사용
  중 (사용자 브라우저 chrome 누적 37k CPU초, msedgewebview2,
  rustdesk 원격 데스크톱 등 상시 부하). 대화형 사용 중인 머신에서
  벤치 수치는 신뢰 불가.
- 기준선(26/23ms, S11/S22)도 같은 머신이지만 당시는 유휴 상태였던
  것으로 보임.

## 결론·후속

- **게이트 판정 보류.** 코드 변경 없음(핀 91b6a7e 원복, 트리 클린).
- 재실측 조건: 머신 유휴 시(브라우저·원격 데스크톱 종료 후) 네이티브
  bench 2회 연속 안정값 + 브라우저 bench-web2/bench-undo.
- 참고: SBOdf 상속 커밋 2건이 원인이라는 초기 가설은 이 데이터로는
  판별 불가 (실행 2가 노이즈에 묻힘).
