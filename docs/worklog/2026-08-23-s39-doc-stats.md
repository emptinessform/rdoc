# 2026-08-23 — S39: 문서 통계 (M2)

## 목표·범위

파일 메뉴 "문서 정보" — 페이지·문단·단어·문자(공백 포함/제외) 수를
상태줄에 표시. 코어 속성(제목/저자)은 후속.

## 한 일

- 포크 무수정. rdoc `RenderCache::page_count()` 노출, wasm
  `doc_stats()` — 본문 문단 + 표는 셀 단위 텍스트로 집계(TableRef에
  text()가 없어 row/col 순회; 셀 텍스트 API는 CellRef.text()).
  페이지 수는 최근 렌더 캐시 기준.
- UI: 파일>문서 정보 → 상태줄 "페이지 N · 문단 P · 단어 W · 문자 C
  (공백 제외 C')". `__t.docStats`.

## 검증 (실측)

- 신규 stats-test 8단정: 페이지 수 = 렌더된 SVG 수, "added " 타이핑
  후 단어 +1·문자 +6·공백 제외 +5 정밀 증가, undo 복원, 메뉴 동작이
  상태줄에 출력.
- 전체 배터리 **42/42 그린**, 네이티브 lib 9/9.

## 한계·후속

- 본문 스토리만 집계 (머리글/각주 텍스트 제외 — Word의 기본 단어
  수와 동일한 관례). 코어 속성 표시 후속.
