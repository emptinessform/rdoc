# 2026-08-22 — S15-1: 브라우저 테스트 스위트 저장소 편입

## 목표·범위

세션 스크래치패드에만 있던 검증 스크립트 27개(기능 25 + 벤치 2)를
`rdocx-svg-poc/web/tests/`로 편입하고 실행 규약을 README로 문서화.
세션이 끝나면 유실되던 검증 자산의 영속화 — 코드 변경 없음.

## 내용

- `web/tests/*.js`: S4~S14의 전 스위트 (IME/프리에딧, 6스토리 편집,
  Enter/병합, 노트 삽입·삭제, 저장 왕복, UX 4종, Shift 선택, 문단 간·
  셀 간 선택, 찾기/바꾸기, 붙여넣기/잘라내기, 정렬/크기/BIU, 벤치 2종).
- `web/tests/README.md`: 실행 방법(브라우즈 스킬 패턴 — /tmp 복사,
  캐시 버스트, 폴링 금지 대기), 규약(한글 음절 세그먼트 좌표 계산,
  wasm-bindgen None=undefined, 데모 경로 상수 의존), 스위트 개요 표.

## 검증

- 저장소 사본 스모크: `web/tests/ime-test.js` → `{"ok":true}`.
- `.gitignore` 미저촉 확인 (web/pkg·out·폰트만 제외).
