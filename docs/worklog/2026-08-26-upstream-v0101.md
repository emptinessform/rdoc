# 2026-08-26 — 업스트림 점검: v0.10.1 출시, #44/#45/#46 전부 종결

## 목표·범위

GitHub 알림(2026-08-24~26)으로 v0.10.1 / rpptx-v0.6.0 출시와 이슈
#44·#46, PR #45 종결이 통지됨. 실제 업스트림 트리에서 사실을 확인하고
S55에서 보류한 이행 재개 가부를 판단한다. 코드 변경은 하지 않는다.

## 한 일 — 포크 클론에서 실측 확인

`D:\sb\SBDoc\rdocx-fork`에서 `git fetch upstream --tags`.

- 새 태그: **v0.10.0**, **v0.10.1**, **rpptx-v0.6.0** (+ s54~s57).
- `v0.9.0..v0.10.1` = **61 커밋**.

### 우리 보고 건의 처리

| 우리 건 | 업스트림 | 커밋 |
|---|---|---|
| #44 `FontFile.family` 무시 | F-X051 "honor caller font family aliases" — PR #45 **미머지**, 하드닝 등가물로 수용 | `0b1861a` |
| #45 레퍼런스 구현 | 위와 동일 (크레딧만) | — |
| #46 에디터 경로 2~3× 회귀 | **F-X052 "restore interactive relayout performance"** | `0015159` |

- F-X052 커밋 메시지: *"Remove retained-payload cloning and redundant
  shaping from editor relayout while preserving exact typed compatibility,
  bounded transactional caches, provenance, and tagged structure."*
  변경량: `rdocx-layout/src/engine.rs` +1685, `paginator.rs` +328,
  `block.rs` +157, `oxml-layout/src/font.rs` +66, `line.rs` +24.
- F-X051 변경량: `oxml-layout/src/font.rs` +567, `rdocx-layout/src/engine.rs`
  +155, `rdocx/src/document.rs` +226, 회귀 테스트 +68.
- v0.10.1 `CHANGELOG.md` **Fixed** 절에 #44/#45/#46이 링크와 함께 명시.
  (같은 릴리스의 PR #47~#50, #52는 타 기여자 건.)

### MarkedContent 마이그레이션 노트 — 반영됨

우리가 #46에 덧붙인 "외부 백엔드가 조용히 빈 렌더가 된다" 지적이
문서화됐다. v0.10.1에서 확인한 위치:

- `CHANGELOG.md` 3곳 — v0.9.0 호환성 노트의 **소급 정정** 포함
  (317~318행: 외부 백엔드는 `MarkedContent::children`를 재귀하거나
  `oxml_layout::walk`를 쓸 것).
- `docs/hld/08-rendering-spec.md` (139/212/661/860행),
  `docs/hld/03-architecture.md` 142행, `docs/hld/10-bindings-spec.md` 306행.

### 이행에 영향을 주는 API 변화

- `transfer_reusable_bundled_fallback_layout_from`이 v0.10.1
  `crates/rdocx/src/document.rs:4618`에 존재 → S55 적응 패치의 엔진
  핸드오프 교체가 그대로 유효.
- **신규** `transfer_reusable_bundled_fallback_layout_from_with_aliases`
  (`document.rs:4646`) → 핸드오프 + caller 폰트 별칭(malgun)을 한 번에
  처리 가능. 이행 시 이 변형 채택을 검토한다.
- `resize_inline_image`는 v0.10.1에 **없음** → 우리 에디터 스택 커밋은
  포크 유지가 계속 필요.

## 판단

업스트림이 메일/이슈에 게시한 F-X052 수치(cold-layout 1.02~1.11,
타이핑 0.97~1.15, undo 1.07~1.22, 표 변형 1.03~1.14, 58페이지 동일,
전부 승인 예산 1.25 이내)는 **업스트림 자체 벤치 값**이다. S55에서
우리가 실측한 회귀는 브라우저 타이핑 3.0×·undo 4.0×였으므로,
**우리 벤치로 재측정하기 전에는 해소로 기록하지 않는다.**

또한 S55 함정 그대로 `[patch."<git url>"]`은 rev 고정 의존성에 적용되지
않으므로, A/B는 반드시 **path 의존성 임시 교체**로 세팅한다.

## 후속

→ **S56: v0.10.1 이행 재스파이크** (사용자 승인 2026-08-26).
포크 브랜치 `svg-poc-0.10` 재조립 + rdoc 적응 재적용 + 0.8 핀 대비
동일 세션 교차 측정. 판정 전까지 프로덕션 핀(29a4a5c)은 유지.
