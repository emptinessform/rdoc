# 2026-08-23 — S35: 페이지 설정 (M2 착수)

## 배경

M1 일단락(글꼴/크기/색/B·I·U/정렬/스타일/리스트/링크/줄 간격/표
구조+병합/툴바 상태/Tab). M2(문서 기능) 첫 단계로 페이지 설정 선택.

## 목표·범위

용지(A4/Letter/Legal), 방향(세로/가로), 여백 프리셋(기본/좁게/넓게)
— 파일 메뉴, 각 1 undo. 문서 최종 섹션 대상(v1).

## 한 일

- 업스트림에 set_page_size/set_landscape/set_portrait/set_margins와
  읽기용 section_properties가 **모두 이미 있어 포크 무수정**
  (핀 d93cd83 — 중복 게터를 추가했다 되돌린 수정 커밋 포함).
- wasm 4종: `page_info()`(pt 단위 JSON, 읽기), `set_paper(w,h)` —
  세로 기준 치수를 받고 **현재 방향 보존**, `set_orientation(landscape)`,
  `set_margins_pt(t,r,b,l)` (범위 가드, 각 1 undo).
- UI: 파일 메뉴에 용지 3종/방향 2종/여백 3종 프리셋. `__t`
  pageInfo/setPaper/setOrientation/setMargins.

## 검증 (실측)

- 신규 pagesetup-test 10단정: 가로 전환 시 pageInfo와 **렌더된 SVG
  viewBox가 함께 스왑** → undo 복원, Letter 적용 시 612×792 박스 +
  방향 보존, 좁은 여백 적용 시 첫 본문 히트 x가 왼쪽으로 이동(실측)
  + ml=36 보고, 저장 왕복 보존.
- 전체 배터리 **39/39 그린**, 네이티브 lib 9/9 + corpus 14/14.

## 사고·교훈 (2건, 같은 뿌리)

- `cargo build | tail`/`wasm-pack | tail` 파이프가 실패 exit code를
  가려, ① 중복 게터 커밋이 빌드 실패인 채 포크에 푸시됨(직후 수정
  커밋 d93cd83), ② wasm 빌드 실패를 성공으로 오인(구 산출물로
  스위트 실행, d.ts에 API 부재로 발각). **이후 빌드 체인은
  `${PIPESTATUS[0]}` 검사 필수.**
- wasm 실패 원인: `--no-default-features` 빌드에선 rdocx_oxml이
  직접 의존성이 아님 — 타입 명시를 지우고 추론으로 해결.

## 한계·후속

- 다중 섹션 문서는 최종 섹션만 변경 (섹션별 설정 후속).
- 사용자 지정 치수 입력 UI 없음 (프리셋만).
