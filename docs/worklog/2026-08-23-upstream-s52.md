# 2026-08-23 — 업스트림 응답: #40·#41 종결 (S52 대량 수용)

## 응답 내용 (메인테이너 코멘트, 메일 수신)

- **PR #40 종결 — "addressed"**: 공유 폰트 바이트·공유 페이지 프레임은
  **F-X039**(c69ef66)로 수용(백엔드·전송 안전성·wasm·패키지·해시
  커버리지 확장판). 엔진 핸드오프는 우리 `take/set_layout_engine`(무검증
  setter — 시스템 폰트 엔진이 번들 전용 경로에 꽂힐 위험) 대신 **검증형
  `Document::transfer_reusable_layout_from`** 으로 안착.
- **PR #41 종결 — "fully addressed"**: 재시작 안전 페이지네이터 +
  트랜잭션형 타입 표 캐시 = **F-X040**(3507f37). 직접 머지하지 않은
  이유: 우리 해시 키 비트랜잭션 캐시가 강화된 캐시 안전·메모리 바운드
  계약과 충돌. 나머지는 **F-X043~F-X047**로: 번들 폴백 caller-font
  파사드(e7f8d6f), 에디터 규모 캐시 조회(4bc1b75), 머리글/꼬리글
  재사용(4b3d918), 치환 페이지 정확 재사용(9f1abf3), 빈 문단 속성
  부여(fe026c5 — 우리 폭 0 세그먼트의 강화판).

## 정찰 (sprint/s52 브랜치)

- 위 전부 + **F-X041(#23 글리프 중복 수정)** + F-X042(HF PDF 검증)가
  sprint/s52에 존재. main 미머지(스프린트 리뷰 2차까지 기록 — 머지
  임박 추정).
- s52 API 확인: `layout_with_fonts_and_bundled_fallback` 동명 파사드
  존재(F-X043), `transfer_reusable_layout_from` +
  `transfer_reusable_bundled_fallback_layout_from`(검증형 핸드오프).
  **에디터 API(paragraph_at_path_mut 등)는 없음** — 우리 포크 고유.

## 영향 평가

- 우리 포크의 **성능 커밋 계열(7~8개)이 전부 업스트림 강화판으로 대체
  가능** → 포크가 "에디터 API + dense-form 레이아웃"만 남기고 대폭
  얇아질 수 있음 (포크 유지비 급감 — 이 프로젝트의 원래 전략 완성형).
- rdoc 수정 필요 지점: wasm `replace_doc`의 take/set_layout_engine →
  `transfer_reusable_layout_from`(+bundled 변형) 교체.
- **PR #43**은 폐기된 #41 브랜치에 스택 — 새 베이스(s52 또는 머지 후
  main) 위 리베이스 필요. 빈 문단 메트릭 훅은 F-X047 구현에 맞춰 재작성.
- F-X041 글리프 수정: #23에서 약속한 SVG 백엔드 재검증 협조 — 마이그레이션
  스파이크에서 함께 확인 후 코멘트 후보.

## 다음 단계 (게이트 제안)

s52 기반 스파이크: 새 포크 브랜치 svg-poc-s52 = s52 + 에디터 API 스택
재적용 + dense-form 5커밋 리베이스 (성능 커밋 폐기), rdoc 핸드오프 API
교체, 전 스위트 회귀, F-X041 SVG 재검증. #43 리베이스 푸시는 PR에
보이는 행위라 별도 확인 후.
